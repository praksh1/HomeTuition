auto-install-peers=false
strict-peer-dependencies=false

# Note: pnpm-specific settings (nodeLinker, etc.) live in pnpm-workspace.yaml as of pnpm 10+.
# Putting them here has no effect — `pnpm config get node-linker` returns undefined.
